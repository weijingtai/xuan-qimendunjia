/// 三元（上元 / 中元 / 下元）
///
/// 月家与年家共用此枚举，但**起局宫映射不同**：
/// - 月家（按年支孟仲季）：上元坎1 / 中元兑7 / 下元巽4
/// - 年家（按 60 年三元）：上元坎1 / 中元巽4 / 下元兑7
///
/// 调用方应根据家维度使用相应的映射函数，不要混用。
enum SanYuanType {
  SHANG('上元'),
  ZHONG('中元'),
  XIA('下元');

  final String name;
  const SanYuanType(this.name);
}
